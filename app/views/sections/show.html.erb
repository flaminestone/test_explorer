<p id="notice"><%= notice %></p>

<%= link_to 'Create New Test', new_section_test_path(@section) %>
<p>
  <strong>Name:</strong>
  <%= @section.name %>
</p>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @tests.each do |test| %>
      <tr>
        <td><%= test.name %></td>
        <td><%= link_to 'Show', section_test_path(@section, test) %></td>
        <td><%= link_to 'Edit', edit_section_test_path(@section, test) %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_section_path(@section) %> |
<%= link_to 'Back', sections_path %>
